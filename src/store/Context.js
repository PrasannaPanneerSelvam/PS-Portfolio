import { atom } from 'nanostores';

export const isHeroAnimPending = atom(true);
export const currentPageIndex = atom(0);
export const isInContactPage = atom(false);
export const isHamburgerOn = atom(false);
